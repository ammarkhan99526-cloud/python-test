name: Deploy Python App

on:
  push:
    branches: [ main ]  # Runs when code is pushed to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest  # Uses a free GitHub Linux server
    
    steps:
      # 1. Checkout your code
      - name: Checkout code
        uses: actions/checkout@v4  # Official GitHub action to copy your repo
